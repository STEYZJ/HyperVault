# HyperVault Strategy Search

Use HyperVault as the source of truth for paper strategy and research experience:

```bash
conda run -n HyperVault python -m framework.cli strategy-search --query "$ARGUMENTS"
```

Prefer evidence-backed strategy lessons over paper summaries.
